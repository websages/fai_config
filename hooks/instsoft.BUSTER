#!/bin/bash
DEBIAN_FRONTEND=noninteractive apt-get install -y -o DPkg::options::=--force-confdef -o DPkg::options::=--force-confold aptitude
[ -s $target/etc/kernel-img.conf ] || fcopy -Bi /etc/kernel-img.conf
